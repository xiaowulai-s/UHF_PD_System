# 工作日志 — 2026-06-10

> 项目: 超高频局部放电在线监测系统 (UHF-PD-Monitor)
> 版本: v1.0.5 → v1.0.6
> 日期: 2026-06-10 (Tuesday)

---

## 一、代码变更概要

| 操作 | 数量 | 说明 |
|------|------|------|
| 新增文件 | 0 | — |
| 修改文件 | 7 | `__init__.py`（5 个）+ `pd_main_window.py` + `Update.md` |
| 删除文件 | 138 | 两轮清理总计 |
| 新增目录 | 0 | — |
| 删除目录 | 7 | 空目录残留 |

---

## 二、详细变更内容

### 2.1 代码变更（修改 7 个文件）

#### `ui/pd_main_window.py` — 重构"关于"对话框
- **新增** `CHANGELOG` 类属性：结构化存储 v1.0.1 ~ v1.0.5 共 5 个版本的变更记录
- **重写** `_show_about()`：从简单的 QMessageBox 改为自定义 QDialog + QTabWidget
  - Tab 1 "关于"：展示应用图标 + 版本 + 构建日期 + 系统描述 + 技术栈
  - Tab 2 "更新日志"：滚动可浏览 HTML 格式日志，颜色区分新增（绿）/优化（黄）/修复（红）
- **修复** DT 常量引用错误：`DT.C.ACCENT` → `DT.C.ACCENT_PRIMARY`, `DT.C.BORDER` → `DT.C.BORDER_DEFAULT`

#### `pd_main.py` — 设置程序图标
- 新增导入：`from PySide6.QtGui import QIcon`
- 新增：`app.setWindowIcon(QIcon("assets/icons/ems.png"))`

#### 5 个 `__init__.py` 清理过时的导出
| 文件 | 变更 |
|------|------|
| `core/services/__init__.py` | 移除 AuditLogService, DataQualityService, RemoteAPIService, ReportService, MCGSService；移除对应 `__all__` 项 |
| `core/utils/__init__.py` | 移除 AlarmNotificationService |
| `core/data/__init__.py` | 移除 AlarmRulePersistenceManager |
| `ui/__init__.py` | 从 `MainWindow` 改为 `PDMainWindow`，移除动态导入逻辑 |
| `ui/controllers/__init__.py` | 清空所有旧控制器引用 |
| `ui/widgets/__init__.py` | 移除 `visual` / `ae_parameters_widget` / `ae_scatter_widget` 引用 |

### 2.2 删除文件（138 个）

#### 第一轮：109 个文件

| 类别 | 数量 | 代表文件 |
|------|------|----------|
| 死代码（零引用） | 20 | `ui/modbus_generator_widget.py`, `ui/command_terminal.py`, `core/protocols/protocol_registry.py` 等 |
| 孤立服务 | 3 | `core/services/report_service.py`, `data_quality_service.py`, `alarm_notification.py` |
| 旧系统遗留 | 16 | `main.py`, `ui/main_window.py`, `MCGS.md`, `core/engine/` 3个, `core/plugins/` 3个, `core/enums/` 1个, `core/device/simulator.py`, `core/utils/mcgs_modbus_reader.py` 等 |
| 过期文档 | 46 | `docs/` 全部（.rst/.md 文档，Sphinx 配置等） |
| 过期资源 | 8 | `assets/` 4 个旧图标, `data/` SQLite WAL 文件, `requirements-docs.txt`, `requirements_v2.txt` |
| 级联孤儿 | 17 | 因 `main_window.py` 删除产生的：对话框/控制器/面板等 |
| 其他 | 2 | `.workbuddy/`, `discharge_classification.md` |

#### 第二轮：29 个文件 + 7 个空目录

| 类别 | 数量 | 代表文件 |
|------|------|----------|
| 死代码 | 7 | `core/processing/pipeline.py`, `ae_localizer.py`, `core/data/cleanup_scheduler.py`, `ui_preferences_service.py` 等 |
| 孤儿 | 1 | `core/utils/permission_manager.py` |
| AE 声学（仅测试） | 4 | `ae_envelope.py`, `ae_parameter_extractor.py`, `ae_simulator.py`, `anomaly_detector.py` |
| MCGS 旧架构残留 | 6 | `device_manager.py`(旧版), `fault_recovery_manager.py`, `gateway_model.py`, `interfaces.py` 等 |
| UI 孤立 | 4 | `ui/pages/ae_page.py`, `ae_parameters_widget.py`, `ae_scatter_widget.py`, `visual.py` |
| 空目录 | 7 | `core/engine/`, `core/plugins/`, `core/enums/`, `ui/controllers/`, `ui/core/`, `ui/panels/`, `docs/` |

### 2.3 输出文档

| 文件 | 说明 |
|------|------|
| `工程清理报告.md` | 完整两轮清理报告（已删除清单 + 第二轮可删除清单 + 保留架构 + 统计） |
| `Update.md` | 更新：基准版本 v1.0.5, #11 标记已修复, 新增 UI 布局审计 26 个, 统计更新 |

---

## 三、架构变化

### 清理前（~250 个文件）

```
core/                      ui/
├── engine/ (3个)          ├── main_window.py ★
├── plugins/ (3个)         ├── add_device_dialog.py
├── enums/ (1个)           ├── device_type_dialogs.py
├── protocols/             ├── batch_operations_dialog.py
│   ├── protocol_registry.py
│   ├── base_protocol.py
│   └── types.py
└── ... (共 ~130 个)
```

### 清理后（~150 个文件）

```
core/                      ui/
├── communication/         ├── pd_main_window.py ★
├── data/                  ├── pd_controller.py
├── device/                ├── design_tokens.py
├── foundation/            ├── theme_manager.py
├── processing/            ├── pages/ (7个)
├── protocols/             ├── widgets/ (7个)
├── services/              └── styles/
├── utils/
└── ... (共 ~80 个)
```

**关键变化：**
- 旧工业设备管理系统（MCGS/Modbus 通用）入口 `main.py` 和 `main_window.py` 已移除
- PD 系统成为**唯一入口**：`pd_main.py`
- `docs/` 文档全部删除，后续需重新规划
- 保留的 `core/device/` 中仍有约 25 个 MCGS 遗留文件（上一轮未删除，后续可清理）

---

## 四、工作量统计

| 任务 | 预估耗时 | 实际耗时 | 说明 |
|------|---------|---------|------|
| 工程架构扫描分析 | — | — | 交叉引用 80+ 个 .py 文件 |
| 第一轮文件清理（109 个） | — | — | 手动逐文件确认引用关系 |
| `__init__.py` 清理（6 个） | — | — | 逐一修正导出声明 |
| 第二轮文件清理分析 | — | — | 发现 22 个文件 + 7 个空目录 |
| `工程清理报告.md` 编写 | — | — | 完整架构文档 |
| `Update.md` 更新 | — | — | 标记 #11 已修复 + UI 审计 |
| 关于对话框重构 | — | — | 新增 CHANGELOG + 双 Tab 页 |

---

## 五、待办事项

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 确认并删除第二轮 22 个文件 | 报告已生成，等待用户确认执行 |
| P1 | 补充 `README.md` | 当前可能引用已删除的 docs/ 内容 |
| P2 | 重新规划 `docs/` 文档体系 | Sphinx 已全部删除 |
| P2 | `core/device/` 剩余 MCGS 残留 | 约 25 个文件仍可进一步清理 |
| P2 | 版本更新为 v1.0.6 | `core/version.py` 和 `pyproject.toml` |

---

## 六、备注

1. 本次清理基于**静态交叉引用分析**，确保所有删除文件在主程序运行时均无被引用
2. 测试文件（`tests/`）保留 MCGS 相关测试暂未删除（用户明确要求保留）
3. `app_styles.py`, `animation_scheduler.py`, `window_managers.py`, `serial_utils.py` 等文件因用户保留需求未删除
4. `工程清理报告.md` 已输出，可作为后续清理和归档的参考依据